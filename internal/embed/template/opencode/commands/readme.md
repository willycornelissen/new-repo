---
description: Gera ou atualiza o README.md do projeto na raiz, usando docs-writer para analisar a estrutura, skills e comandos.
---

Load the docs-writer skill. Explore the project structure, examine `.opencode/commands/` and `.opencode/skills/`, read `SKILLS.md` and `AGENTS.md`, then generate or update `README.md` in the project root.

$ARGUMENTS

Generate `README.md` at the root of the project with: project name and description, directory structure, installed skills table, available commands table, and any other relevant sections. Match the existing language and style of the current README if one exists.
