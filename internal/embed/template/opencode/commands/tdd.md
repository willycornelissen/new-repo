---
description: Create a Technical Design Document (TDD) for a project, enriched by specification/explore.md if available. Saves the output to specification/tdd.md.
---

Load the technical-design-doc-creator skill. Then check if specification/explore.md exists. If it does, read it for context. Create a Technical Design Document for the following project, incorporating any relevant findings from specification/explore.md:

$ARGUMENTS

At the end, save the complete TDD to `specification/tdd.md`.
